<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Artworks for: {{APP_NAME}}</title>
        <link rel="stylesheet" href="{{STYLE}}.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:ital,wght@0,600;0,900&display=swap" rel="stylesheet">
    </head>

    <body>
        <div class="artwork" id="grid">
            <h1>{{APP_NAME}}</h1>
            <div class="divider"></div>
            <img src="./{{IMAGE_NAME}}">
            <div class="bottom">
                <p id="publisher">An <b>{{APP_PUBLISHER}}</b><br>Production</p>
                <p id="release"><br>{{APP_RELEASE}}</p>
            </div>
        </div>
        <div class="artwork" id="p">
            <img src="./{{IMAGE_NAME}}">
            <div class="divider"></div>
            <div class="text">
                <h1>{{APP_NAME}}</h1>
                <div class="bottom">
                    <p id="publisher">An <b>{{APP_PUBLISHER}}</b><br>Production</p>
                    <p id="release">{{APP_RELEASE}}</p>
                </div>
            </div>
        </div>
        <div class="artwork" id="hero">
            <img src="./{{IMAGE_NAME}}">
            <div class="divider"></div>
            <div class="text">
                <h1>{{APP_NAME}}</h1>
                <div class="bottom">
                    <p id="publisher">An <b>{{APP_PUBLISHER}}</b><br>Production</p>
                    <p id="release">{{APP_RELEASE}}</p>
                </div>
            </div>
        </div>
    </body>
</html>